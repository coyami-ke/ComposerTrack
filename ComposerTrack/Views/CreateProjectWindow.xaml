<Window x:Class="ComposerTrack.Views.CreateProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:ComposerTrack.ViewModels"
        mc:Ignorable="d"
        Title="ComposerTrack - Create project" Height="450" Width="500" Background="#FF121213" WindowStyle="None"
        d:DataContext="{d:DesignInstance Type=local:CreateProjectVM}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Background="#FF101113">
            <Grid>
                <Image Height="48" HorizontalAlignment="Right" Source="/ComposerTrack;component/Assets/close-window.png" MouseDown="closeWindow_MouseDown"/>
            </Grid>
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0.5" BlurRadius="2"/>
            </Border.Effect>
        </Border>
        <Border Background="#FF101113" Grid.Row="1" CornerRadius="5" Margin="0,5,0,0">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0.5" BlurRadius="2"/>
            </Border.Effect>
        </Border>

        <TextBlock Text="Name project" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Row="1" Margin="20,-250,0,0" Foreground="White"/>
        <TextBox Height="40" Width="300" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="120,-250,0,0" Text="{Binding Path=CreateProjectModel.Name}"/>

        <TextBlock Text="ID project" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.Row="1" Margin="20,128,0,0" Foreground="White"/>
        <TextBox Height="40" Width="300" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="120,116,0,0" Text="{Binding Path=CreateProjectModel.ID}"/>

        <TextBlock Text="Author" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.Row="1" Margin="20,185,0,0" Foreground="White"/>
        <TextBox Height="40" Width="300" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="120,177,0,0" Text="{Binding Path=CreateProjectModel.Author}"/>

        <Button Content="Create project" HorizontalAlignment="Right" VerticalAlignment="Bottom" Grid.Row="1" Margin="20" Height="40" Width="200" Command="{Binding Path=CreateCommand}"/>
    </Grid>
</Window>
